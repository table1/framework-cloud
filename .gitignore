/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.DS_Store
.env
.env.backup
.env.production
# The per-machine runtime variants sit beside .env and hold the same credentials.
.env.herd
.env.docker
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Ansible app vault — never committed, even encrypted
/ansible/vault/

# Loose in the working tree, mode 0600, and named by nothing in the repo. A
# plaintext secret store must not become stageable by a `git add -A`.
/secrets.yml
# A pre-scrub history bundle: an artifact, not source.
/pre-scrub-backup.bundle
